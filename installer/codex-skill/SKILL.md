# Trading 212 MCP Server Skill

This skill provides access to the local Trading 212 MCP server for account, order, history, and instrument tasks.

## Usage

```
@trading212-mcp <prompt>
```

## Configuration

The skill connects to the local MCP server at `http://localhost:<port>` where `<port>` is configured during installation.
